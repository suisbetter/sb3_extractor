# Deps are declared in pyproject.toml's [project.optional-dependencies].
# This file is intentionally empty / kept only for legacy tooling that looks for
# requirements.txt; the package itself has no third-party runtime dependencies.
